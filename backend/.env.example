# Backend port (parent dashboard)
PORT=3000

# Minecraft server to connect to
MC_HOST=localhost
MC_PORT=25565
MC_VERSION=1.20.1

# Name Alex shows up as in-game
BOT_NAME=Alex

# Optional: lock Alex to one specific player by their Minecraft username.
# If not set, Alex follows whoever speaks or joins the server first.
# TARGET_PLAYER=YourChildsUsername

# Optional: add your Anthropic API key for full Claude AI responses.
# Without it, Alex uses smart built-in fallback responses.
# Get a key at: https://console.anthropic.com
# ANTHROPIC_API_KEY=sk-ant-...
